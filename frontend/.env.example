# Backend API base URL (no trailing slash).
# - Leave unset in production when API is on same domain (e.g. Nginx proxies /api to backend).
# - For local dev, set so the frontend can reach the backend:
NEXT_PUBLIC_API_URL=http://localhost:4000
